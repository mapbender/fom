{% extends "FOMUserBundle:Login:box.html.twig" %}
{% block css %}
  <link rel="stylesheet" href="{{ path('mapbender_core_application_assets', {'slug': 'mb3-login', 'type': 'css'}) }}"/>
{% endblock %}
{% block title %} {{ 'fom.user.password.send.title' | trans}} {% endblock %}

{% block box_content %}
    !!!!
    <div class="loginBox">
        <h2 class="contentTitle">{{ 'fom.user.password.send.caption' | trans}}</h2>
        <p class="description">{{ 'fom.user.password.send.email_been_sent' | trans }}</p>
        <a class="left iconLinkButton linkButton" href="{{ path('fom_user_login_login') }}">{{ "fom.user.password.send.back_to_login"|trans}}</a>
        <div class="clearContainer"></div>
    </div>
 <script type="text/javascript">
    var Mapbender = Mapbender || {};
    Mapbender.configuration = {elements:{}};
  </script> 
{% endblock %}